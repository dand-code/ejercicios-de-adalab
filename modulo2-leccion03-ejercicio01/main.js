'use strict';

// avatar por defecto
const DEFAULT_AVATAR = 'http://placehold.it/300x300';
// avatar que eligió el usuario al registrarse
let userAvatar = 'http://www.fillmurray.com/300/300';
const imageAvatar = document.querySelector(".user__avatar")

if (userAvatar === ''){
  imageAvatar.src = DEFAULT_AVATAR;
} else {
  imageAvatar.src = userAvatar;
}

// imageAvatar.src = userAvatar === '' ? DEFAULT_AVATAR : userAvatar;

const  = document.querySelector('');
const nombre = document.querySelector.innerHTML('');

const name = document.querySelector.innerHTML('.hola');

 = document.querySelector.innerHTML('');